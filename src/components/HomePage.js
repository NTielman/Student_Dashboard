import React from 'react';
import Charts from './Charts';
import Sidebar from './Sidebar';

const HomePage = () => {
    return (
        <div>
            <Charts />
            <Sidebar />
        </div>
    );
}

export default HomePage;